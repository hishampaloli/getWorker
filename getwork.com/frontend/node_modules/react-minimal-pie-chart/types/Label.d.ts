import type { CSSProperties, SVGProps } from 'react';
import type { ExtendedDataEntry } from './commonTypes';
export declare type LabelRenderProps = {
    x: number;
    y: number;
    dx: number;
    dy: number;
    textAnchor: string;
    dataEntry: ExtendedDataEntry;
    dataIndex: number;
    style?: CSSProperties;
};
declare type Props = SVGProps<SVGTextElement> & LabelRenderProps;
export default function ReactMinimalPieChartLabel({ dataEntry, dataIndex, ...props }: Props): JSX.Element;
export {};
